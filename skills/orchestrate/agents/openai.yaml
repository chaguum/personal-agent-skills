interface:
  display_name: "Orchestrate"
  short_description: "Piloter un plan via des sous-sessions"
  default_prompt: "Use $orchestrate to design and execute this project through sequential subsessions."

policy:
  allow_implicit_invocation: false
